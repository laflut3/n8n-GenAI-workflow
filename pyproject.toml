[project]
name = "genai-labs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
city-score-mcp = "genai_labs.mcp_server:run"
